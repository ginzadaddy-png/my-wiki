streamlit>=1.40
anthropic>=0.40
python-frontmatter>=1.1
python-dotenv>=1.0

# Phase 2 — RAG (의미 검색)
# 벡터 검색은 numpy brute-force (core/vectorstore.py). chromadb의 영속 HNSW
# 인덱스 reload 불안정성('Error finding id')을 피하려 제거. ~1,400벡터엔 numpy로 충분.
numpy>=1.26
sentence-transformers>=3.0
# torch는 sentence-transformers 의존성으로 자동 설치 (CPU판)
